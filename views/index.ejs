<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>HUNE B2B Preview</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body{font-family:Arial;max-width:900px;margin:40px auto;line-height:1.4;padding:0 16px}
    .row{display:flex;gap:10px;flex-wrap:wrap;margin:12px 0}
    .btn{padding:10px 12px;border-radius:10px;border:1px solid #ddd;background:#fff;cursor:pointer;font-weight:700}
    .btn.primary{background:#009bb5;color:#fff;border:0}
    input{width:100%;padding:10px;margin:6px 0 14px;border:1px solid #ddd;border-radius:10px}
    small{color:#666}
  </style>
</head>
<body>
  <h2>HUNE B2B Preview</h2>

  <div class="row">
    <form method="POST" action="/generate">
      <input type="hidden" name="clientUrl" value="<%= clientDefault %>">
      <input type="hidden" name="huneUrl" value="https://www.hune.eco/products/halcon-w10">
      <button class="btn" type="submit">Halcon W10</button>
    </form>

    <form method="POST" action="/generate">
      <input type="hidden" name="clientUrl" value="<%= clientDefault %>">
      <input type="hidden" name="huneUrl" value="https://www.hune.eco/collections/auriculares/products/flora">
      <button class="btn" type="submit">Flora</button>
    </form>

    <form method="POST" action="/generate">
      <input type="hidden" name="clientUrl" value="<%= clientDefault %>">
      <input type="hidden" name="huneUrl" value="https://www.hune.eco/collections/cargadores/products/huron-65w">
      <button class="btn" type="submit">Huron 65W</button>
    </form>
  </div>

  <hr style="border:0;border-top:1px solid #eee;margin:18px 0">

  <form method="POST" action="/generate">
    <label><b>Cliente PDP</b></label><br/>
    <input name="clientUrl" required value="<%= clientDefault %>" />

    <label><b>Hune PDP</b></label><br/>
    <input name="huneUrl" required value="<%= huneDefault %>" />

    <button class="btn primary" type="submit" style="width:100%">Generar</button>
  </form>

  <p><small>Tip: si en Render dice “Playwright disabled…”, igual te genera HTML, solo que sin screenshot.</small></p>
</body>
</html>
